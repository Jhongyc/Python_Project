# Author:GaoYuCai
#模块即库
#标准库（直接使用）和第三方库（需要安装，例如Dango）
#import  sys
#print(sys.path)#打印环境变量
#print(sys.argv)#打印相对路径
#会先到当前路径先导入
import os
#cmd_res=os.system("dir")
#print(cmd_res)
#打印当前路径下的文件
#直接输出到屏幕上，输出完就结束了
#只执行命令，不保存结果
#cmd_res=os.popen("dir").read()
#print(cmd_res)
#os.mkdir("new_dir")
